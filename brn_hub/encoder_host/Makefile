
include $(APPDIR)/Make.defs
include $(SDKDIR)/Make.defs

PROGNAME = $(CONFIG_BRN_HUB_ENCODER_HOST_PROGNAME)
PRIORITY = $(CONFIG_BRN_HUB_ENCODER_HOST_PRIORITY)
STACKSIZE = $(CONFIG_BRN_HUB_ENCODER_HOST_STACKSIZE)
MODULE = $(CONFIG_BRN_HUB_ENCODER_HOST)

ASRCS =
CSRCS =
MAINSRC = encoder_host_main.c common.c

include $(APPDIR)/Application.mk
